const welcomeRoutes = require('./hello.routes');
const usersRoutes = require('./users.routes');
const addressesRoutes = require('./addresses.routes');

module.exports = {
  welcomeRoutes,
  usersRoutes,
  addressesRoutes,
}